package model;



/**
 * Enum representing the different asset classes in a portfolio.
 * Using an enum ensures type safety and prevents errors from using plain strings.
 */
public enum AssetClass {
    STOCKS,
    BONDS,
    CASH
}